Main Topic

1 - Mathematical Operators

2 - List, Tuples or Dictionaries ?

  First, List and tuplea are almost the same thing. The difference betewwen each order is that list are mutable while tuples are not.
  In other words, instead of tuples, you can change a element inside of a list.
  There are diferences in the each structure too. List uses brackets  while tuples uses the parenthesis.
  
  For example:
  
  >>>> Changing a list:
  
list = [0, 1, 2, 3]
print(lista)
[0, 1, 2, 3]
list[0] = 1
print(list)
[9, 1, 2, 3]

  >>>> Changing a tuple:
  
tuple = (0, 1, 2, 3)
print(tupla)
(0, 1, 2, 3)
tuple[0] = 1
TypeError: 'tuple' object does not support item assignment

 Dictionaries: Dictionaries are one way to overcome the list limits as the fact that the elemets are independents of each other. 
 With dicionary, the entire collection of data in which a key is stored has a corresponding value just lika a dictionary in real life.
 
 
3 - Methods & Objetcs

  Everthing in python is a objetc.
  a method is a function that "belongs" to an object
  
4 - TXT, CSV and JASON files: What is the differrence?
  TXT = A txt file is a simple file with no formartting such as bold,italic or different fonts. They are universal an can be read in any device or operating system. 
  
  CSV (Comma Separeted Value) = As the name says, CSV is a file where the data is separeted by comma. They are similiar  TXT.
  
  JASON (Java Script Ojetc Notation) = It is a simple data format that is used to transmit information in web application. This kind of file is like a dictionay 
  structere in python.
  
 5 - Function:

Built in function: examples: reduce, map, filter, zip, enumerate
Lambda Fuction = operação anonima; fazer uma função em tempo de execução
Expressões regulares = "Expressões Regulares são uma sequência de caracteres que definem uma busca padrão em strings. Em Python, as Expressões Regulares são suportadas
pelo pacotere. Ele fornece uma série de funções para pesquisar e substituir padrões em strings. Algumas das tarefas mais comuns que podem ser realizadas com Expressões
Regulares incluem verificar se uma string corresponde a um determinado padrão, extrair informações de uma string combase em um padrão específico e substituir trechos
de uma string com base em um padrão.Por  exemplo,  você  pode  usar  uma  expressão  regular  para  verificar  se  uma  string representa um endereço de e-mail válido,
ou para encontrar todas as ocorrências de um padrão em uma string."
